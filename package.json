{
  "devDependencies": {
    "@types/node": "^14.6.2",
    "gts": "^2.0.2",
    "typescript": "^4.0.2",
    "yarn-upgrade-all": "^0.5.4"
  },
  "scripts": {
    "check": "gts check",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "yarn run compile",
    "pretest": "yarn run compile",
    "posttest": "yarn run check"
  }
}
